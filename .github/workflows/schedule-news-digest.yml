---
name: Scheduled News Digest
on:
    workflow_dispatch:
    schedule:
        - cron: "46 8 * * *"
permissions:
    contents: write
    pages: write
    id-token: write
    pull-requests: read
    issues: read

jobs:
    build:
        outputs:
            changed: ${{ steps.commit.outputs.changed }}
            ref: ${{ steps.commit.outputs.ref }}
        permissions:
            contents: write
            pull-requests: read
        runs-on: ubuntu-latest
        steps:
            - name: Check out Repo
              uses: actions/checkout@v7

            - name: Set up Python
              uses: actions/setup-python@v7
              with:
                  python-version: 3.13.2

            - uses: actions/cache@v6
              name: Configure pip Caching
              with:
                  path: ~/.cache/pip
                  key: ${{ runner.os }}-pip-${{ hashFiles('_python/requirements.txt') }}
                  restore-keys: |
                      ${{ runner.os }}-pip-

            - name: Install Python Dependencies
              run: |
                  python -m pip install -r _python/requirements.txt

            - name: Build daily news digest
              run: |-
                  python '_python/news-digest.py'

            - name: Commit and push if changed
              id: commit
              run: |-
                  git diff
                  git config user.name "GitHub Action"
                  git config user.email "action@github.com"
                  git add -A

                  if git diff --staged --quiet; then
                    echo "changed=false" >> "$GITHUB_OUTPUT"
                    exit 0
                  fi

                  git commit -m "Chore: Daily news digest for $(date +'%Y-%m-%d')"
                  # main moves on while this job runs (the Pi pushes every 5 minutes),
                  # so rebase onto the latest main and retry rather than losing the data.
                  pushed=false
                  for attempt in 1 2 3; do
                    if git pull --rebase --autostash && git push; then
                      pushed=true
                      break
                    fi
                    git rebase --abort 2>/dev/null || true
                    echo "Push attempt $attempt failed; retrying"
                    sleep $((attempt * 10))
                  done
                  if [ "$pushed" != true ]; then
                    echo "::error::Could not push the data update after 3 attempts"
                    exit 1
                  fi
                  echo "changed=true" >> "$GITHUB_OUTPUT"
                  echo "ref=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

    deploy:
        needs: build
        if: needs.build.outputs.changed == 'true'
        uses: ./.github/workflows/deploy-pages.yml
        with:
            ref: ${{ needs.build.outputs.ref }}
        secrets: inherit
