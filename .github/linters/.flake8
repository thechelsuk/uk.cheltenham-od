[flake8]
# Only the checks that find real bugs: syntax errors (E9) and everything pyflakes
# reports (F): undefined names, unused imports and variables. Line length and
# whitespace style are not enforced here; see the comments in linter.yml.
select = E9,F
